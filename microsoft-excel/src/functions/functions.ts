/* global clearInterval, console, CustomFunctions, setInterval */

/**
 * Summarize.
 * @customfunction SUMMARIZE
 * @param rangeString Range string like A1:A9
 * @returns The summarization string
 */
export async function summarize(rangeString: string) {
  return rangeString;
}
